apiVersion: v1
kind: Service
metadata:
  name: okyoubane
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okhosttast"
    port: 8080
  selector:
    app: okyoubane
